{
  "name": "cs-okey",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p packages/engine/tsconfig.typecheck.json && tsc --noEmit -p packages/bot/tsconfig.typecheck.json && tsc --noEmit -p packages/app/tsconfig.typecheck.json && tsc --noEmit -p packages/server/tsconfig.json"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
